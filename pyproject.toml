[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "neural_net"
version = "0.0.1"
description = "A from-scratch neural network library."
requires-python = ">=3.9"
dependencies = [
    "numpy",
]
